# TransientModelSaltMarsh

The model can be run with the script "TransplantSimulationOptimizeParams.m", which is located in the folder TransientDynamicsSimple. Path to the workspace needs to be set manually.

The simulation from Box 3.1 can be found in "Converter Density-Frequency".
